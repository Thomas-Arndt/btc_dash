import React from 'react';

const Main = ({children}) => {
    return (
        <div style={{backgroundColor: '#0E0042', height: '100vh'}}>
            {children}
        </div>
    )
}

export default Main;